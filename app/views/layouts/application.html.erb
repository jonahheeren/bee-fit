<!DOCTYPE html>
<html>
<head>
  <title>Bee-Fit!</title>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <div class="banner">
    <img src="bee-fit.png" style="height: 40px; width: 40px;">
    Bee-Fit, Now!
    <div style="float: right; text-align: center; vertical-align: center;">
    <% if current_user %>
    Hello, <%= current_user.name %> | <%= link_to "Logout", '/logout' %>
    <% else %>
    <%= link_to 'Login', '/home' %> | <%= link_to 'Signup', '/signup' %>
    <% end %>
  </div>
    </div>
</head>
<body>

<%= yield %>

</body>
</html>
